-r ../requirements.txt

# The contents of pyTooling's optional extras, spelled out. 'pyTooling[pypi]' can't be written here: pyTooling is the
# package being documented, so requiring it would install a released copy over the checkout. Every extra a module
# guards its imports with has to appear, because autodoc imports every module it documents.
colorama >= 0.4.6      # 'terminal'
ruamel.yaml ~= 0.19.1  # 'yaml'
setuptools >= 83.0     # 'packaging'

# 'pypi' - read by the 'dependency-table' directive, which resolves the tables in 'Dependency.rst' against PyPI
aiohttp >= 3.12        # aiohttp limited on MSYS2 to 3.12.x
packaging >= 25.0
requests >= 2.32

# Enforce latest version on ReadTheDocs
sphinx ~= 9.1
#docutils ~= 0.22.0
docutils_stubs ~= 0.0.22

# ReadTheDocs Theme
sphinx_rtd_theme ~= 3.1

# Sphinx Extenstions
sphinxcontrib-mermaid ~=2.1
autoapi >= 2.0.1
sphinx_design ~= 0.7.0
sphinx-copybutton >= 0.5.2
sphinx_autodoc_typehints ~= 3.12
sphinx_reports ~= 0.11.0

# Used by the 'XSDGraphviz' extension to read a schema
xmlschema ~= 4.3
