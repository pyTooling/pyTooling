-r ../../requirements.txt

# Static Type Checking
mypy[reports] ~= 2.3
typing_extensions ~= 4.16
types-colorama >= 0.4.15
types-setuptools >= 80.9
lxml >= 6.1, <7.0

# For pyTooling.Configuration.YAML testing
ruamel.yaml ~= 0.19.1

# For pyTooling.TerminalUI testing
colorama ~= 0.4.6

# For pyTooling.Documentation.Sphinx type checking - the subpackage imports Sphinx and docutils, which pyTooling
# itself doesn't depend on. Without it, mypy reports a missing stub for every 'from sphinx import ...'.
sphinx ~= 9.1
